colorBlocks.factory('DataFactory', [function() {
  console.log("data factory running");
  //private
  var colors = ['red', 'blue', 'papayawhip', 'green']
  var newColor = [];

  var addTheColor = function () {
    // var addColor = {color: colors};
    colors.push(addColor);
  }


return {
  colors: colors,


  addTheColor: function(color){
  addTheColor(color);
}
};

}]);

//Hi Score Add a new function based on if correct ++ score. store in a varible
//alert promt for name, store in varible and dispaly [name + score];
